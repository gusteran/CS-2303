/*
 * Board.c
 *
 *  Created on: Sep 9, 2019
 *      Author: gustt
 */

#include "Board.h"

Board makeBlankBoard() {
	int board[ROWS][COLS];
	int x;
	int y;
	Marker marker = {x, y};
	Board Board = { board, marker};//{x,y} };
	Board.marker.x = COLS / 2;
	Board.marker.y = ROWS / 2;
	for (int i = 0; i < ROWS; i++) {
		for (int j = 0; j < COLS; j++) {
			Board.array[i][j] = 0;
		}
	}
	return Board;
}

